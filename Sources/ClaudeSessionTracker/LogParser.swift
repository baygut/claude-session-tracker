// No longer used. The app now reads live usage/limit data directly from
// claude.ai's own API (see ClaudeAPIClient.swift + AuthManager.swift)
// instead of estimating it from local Claude Code transcripts.
